# .env.template
# Template de variables de entorno para la aplicación
#
# Copia este archivo a .env y configura los valores según tu entorno
#
# En Railway/Render, estas variables se configuran en el dashboard web
# NO SUBAS EL ARCHIVO .env A GIT (ya está en .gitignore)

# ==============================================================================
# ENVIRONMENT
# ==============================================================================
# Valores posibles: dev, staging, prod
ENVIRONMENT=dev

# ==============================================================================
# DATABASE
# ==============================================================================
# Development (SQLite local):
DATABASE_URL=sqlite:///./tareas.db

# Production Railway (PostgreSQL):
# DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST].railway.app:6379/railway

# Production Render (PostgreSQL):
# DATABASE_URL=postgresql://[USER]:[PASSWORD]@[HOST].render.com/[DATABASE]

# ==============================================================================
# API CONFIGURATION
# ==============================================================================
API_TITLE=API de Tareas - Cloud Ready
API_VERSION=1.0.0
API_DESCRIPTION=API REST con FastAPI + SQLAlchemy + PostgreSQL en Cloud

# ==============================================================================
# CORS (Cross-Origin Resource Sharing)
# ==============================================================================
# En desarrollo: permite todos los orígenes (automático)
# _CORS_ORIGINS=*

# En producción: especifica solo tus dominios permitidos (separados por comas)
# _CORS_ORIGINS=https://tu-frontend.com,https://www.tu-frontend.com

# ==============================================================================
# SECURITY
# ==============================================================================
# IMPORTANTE: Genera una clave secreta fuerte en producción
# Puedes generarla con: openssl rand -hex 32
SECRET_KEY=change-me-in-production-please-use-openssl-rand-hex-32

# ==============================================================================
# LOGGING
# ==============================================================================
# Valores posibles: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ==============================================================================
# SERVER (Uvicorn)
# ==============================================================================
# Railway y Render asignan el puerto dinámicamente via $PORT
# No necesitas configurar esto en producción
HOST=0.0.0.0
PORT=8000

# ==============================================================================
# DATABASE CONNECTION POOL (PostgreSQL)
# ==============================================================================
# Solo relevante para PostgreSQL en producción
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# ==============================================================================
# MONITORING (Opcional)
# ==============================================================================
# Si usas Sentry para error tracking:
# SENTRY_DSN=https://[PUBLIC_KEY]@[ORG_ID].ingest.sentry.io/[PROJECT_ID]
