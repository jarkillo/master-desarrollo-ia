# Ruff configuration for Modulo 4 Clase 3

[lint]
# Ignore B008 (function calls in argument defaults)
# This is the standard FastAPI pattern for dependency injection:
# async def endpoint(db: Session = Depends(get_db))
# It's safe and recommended by FastAPI documentation

# Ignore ARG001 (unused function arguments)
# The `app` parameter in lifespan is required by FastAPI's signature
# even when not explicitly used in the function body
ignore = ["B008", "ARG001"]
